import styled from 'styled-components';

const DrawerTitle = styled.h2`
  font-size: 28px;
  font-family: 'Play';
  font-weight: normal;
  color: #ffffff;
  opacity: 0.7;
  margin: 0;
`;

export default DrawerTitle